# wahook config — see TECHDOC.md §5 for the full schema reference.

device:
  store: ./wa.db

webhooks:
  - name: local-test
    url: http://localhost:9000/wa
    # headers:
    #   Authorization: Bearer xxx
    timeout: 10s
    retry: 2
    filters:
      groups_only: false
      dm_only: false
      ignore_from_me: false
      ignore_broadcast: true
      senders: []
      keyword_prefix: ""

  # - name: n8n
  #   url: https://n8n.local/webhook/wa
  #   headers:
  #     Authorization: Bearer xxx
  #   filters:
  #     ignore_from_me: true
  #     keyword_prefix: "!cmd"
